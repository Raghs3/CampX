# Email Configuration for CampX Marketplace
# Copy this file to .env and fill in your email credentials

# ============================
# SENDGRID (PRODUCTION) - RECOMMENDED
# ============================
# SendGrid is REQUIRED for production on Render (free tier blocks Gmail/Outlook SMTP)
# Free tier: 100 emails/day - Perfect for CampX Marketplace
# Setup steps:
# 1. Sign up at https://sendgrid.com
# 2. Verify your sender email address (Settings → Sender Authentication)
# 3. Create API Key: Settings → API Keys → Create API Key → Full Access
# 4. Copy the API key (you'll only see it once!)
# 5. Set these environment variables on Render:
SENDGRID_API_KEY=your-sendgrid-api-key-here
SENDGRID_SENDER_EMAIL=your-verified-email@domain.com

# ============================
# NODEMAILER FALLBACK (LOCAL DEVELOPMENT)
# ============================
# For local development, you can use Gmail or Outlook
# If SendGrid variables are missing, the app will fallback to nodemailer
EMAIL_SERVICE=gmail
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password-here

# Session Secret (random string for session encryption)
# Generate a random string for production
SESSION_SECRET=change-this-to-a-random-secret-key

# Database Configuration
# For local development: Leave DATABASE_URL empty (will use SQLite)
# For production: Render will automatically set DATABASE_URL for PostgreSQL
# DATABASE_URL=postgresql://user:password@host:5432/database

# Deployment Configuration
# BASE_URL=https://your-app-name.onrender.com
# NODE_ENV=production

# Example for Gmail:
# EMAIL_SERVICE=gmail
# EMAIL_USER=campx.marketplace@gmail.com
# EMAIL_PASSWORD=abcd efgh ijkl mnop

# Example for Outlook:
# EMAIL_SERVICE=outlook
# EMAIL_USER=campx@outlook.com
# EMAIL_PASSWORD=your-password
